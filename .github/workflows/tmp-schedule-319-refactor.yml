name: Temporary Schedule 319 Refactor

on:
  push:
    branches:
      - "fix/figma-319-schedule"

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Parser check
        run: echo "schedule refactor workflow is valid"
