# Itjima brand assets

Canonical final identity approved 2026-09-14.

- Current asset version: `v8`
- Wordmark: lowercase `itjima` SVG lockup (`itjima-wordmark-v8.svg`) with PNG fallback
- Icon: `>ij<` in black on `#FFF986` (source: padded yellow lockup; favicon + launcher sizes generated from `itjima-icon-master.png`)
- Browser/PWA/install assets use the icon.
- In-product navigation, landing headers, About, and brand lockups use the wordmark artwork.
- `v7` public paths remain as compatibility aliases during the cache migration and serve the current v8 artwork.
- Do not recreate the logo using a substitute UI font or the retired yellow-dot lockup.
- 2026-09-15: replaced a corrupt PNG export and switched landing/app chrome to real `<img>` rendering.
